# Paper's API is versioned with the game, but the surface this adapter uses —
# the scheduler, dispatchCommand, shutdown — has been stable for many years, so
# retargeting is normally just this line.
paper_api_version=1.21.1-R0.1-SNAPSHOT
org.gradle.jvmargs=-Xmx2G
