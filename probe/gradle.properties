# Build target for the loader adapters. Because the version-coupled surface is
# three methods (see docs/PLATFORMS.md), adding a version is normally a change
# to these values and nothing else.
minecraft_version=1.21.1
yarn_mappings=1.21.1+build.3
loader_version=0.16.5
fabric_api_version=0.107.0+1.21.1
org.gradle.jvmargs=-Xmx2G
